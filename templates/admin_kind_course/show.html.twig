{% extends 'admin.html.twig' %}

{% block title %}KindCourse{% endblock %}

{% block body %}
    <h1>KindCourse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kind_course.id }}</td>
            </tr>
            <tr>
                <th>InternalName</th>
                <td>{{ kind_course.internalName }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ kind_course.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_kind_course_index') }}">back to list</a>

    <a href="{{ path('app_admin_kind_course_edit', {'id': kind_course.id}) }}">edit</a>

    {{ include('admin_kind_course/_delete_form.html.twig') }}
{% endblock %}
