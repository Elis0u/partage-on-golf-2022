{% extends 'admin.html.twig' %}

{% block title %}Contact - Partage ton golf{% endblock %}

{% block body %}
    <h1 class="text-center">Demande de contact de {{ contact.firstname }} {{ contact.lastname }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ contact.firstname }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ contact.lastname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact.email }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ contact.subject }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ contact.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_contact_index') }}">Voir tous les formulaire de contact</a>

    {{ include('admin_contact/_delete_form.html.twig') }}
{% endblock %}
